{
  "name": "limbase",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.5.4"
  },
  "scripts": {
    "clean": "rm -rf ./lib",
    "copy-files": "cp -rf ./src/assets/ ./lib/assets",
    "build": "yarn clean && tsc && yarn copy-files"
  }
}
